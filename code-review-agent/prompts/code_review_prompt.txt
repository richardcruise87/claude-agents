You are performing a comprehensive code review for an OpenStack Octavia change.

**Change Information:**
- Repository: {repo_name}
- Change Number: {change_number}
- Patchset: {patchset_display}
- Expected commit SHA: {head_sha}
- Gerrit URL: {GERRIT_BASE_URL}/c/{repo_name}/+/{change_number}
- Local repo: {repo_path}
{specific_patchset_note}{previous_review_section}
---

## Step 1: Verify Checkout

The Python pre-flight has already fetched and checked out patchset {patchset_display}.
Confirm you are on the correct commit before proceeding:

```bash
git -C {repo_path} log -1 --format="SHA: %H  Subject: %s"
```

The SHA **must** match `{head_sha}`. If it does not, output:
```
ERROR: Wrong commit checked out. Expected {head_sha}. Stopping.
```
and do not continue.

---

## Step 2: Change Information (pre-fetched by Python)

The following data was collected by the Python pre-flight. Use it as your
primary source for commit and diff analysis. Use the **Read tool** to examine
specific files in more detail when needed.

### Commit

```
{commit_info_text}
```

### Files Changed

```
{changed_files_text}
```

{patchset_comparison_section}

---

## Step 3: Test Results (pre-run by Python)

The following test suites were run against this patchset before the review started.
Interpret the results and include them in the report — do **not** re-run these commands.

{test_results_text}

---

## Step 4: Comprehensive Code Analysis

Read the modified files carefully using the **Read** tool. Analyse for:

### Code Quality
- **Style**: PEP 8 compliance, naming conventions, code organisation
- **Readability**: Clear logic, appropriate comments, good variable names
- **Complexity**: Are functions too complex? Need refactoring?

### Correctness & Safety
- **Logic**: Are there logical errors or edge cases not handled?
- **Error Handling**: Proper exceptions, validation, edge cases
- **Security**: Vulnerabilities (injection, insecure defaults, credential exposure)
- **Concurrency**: Thread safety if applicable
- **Resource Management**: Proper cleanup, no leaks

### Testing
- **Test Coverage**: Are new/modified code paths tested?
- **Test Quality**: Are tests meaningful and not just for coverage?
- **Missing Tests**: What test scenarios are missing?

### Documentation
- **Docstrings**: Present and accurate?
- **Comments**: Explain complex logic?
- **Commit Message**: Clear, follows OpenStack format?
- **Release Notes**: Needed and included?

### OpenStack Specific
- **API Compatibility**: Breaking changes to APIs?
- **Database Migrations**: Are they needed and included?
- **Configuration**: New config options documented?
- **Deprecations**: Properly marked with warnings?

---

## Step 5: Write the Review Document

Write the complete review to **exactly** this path using the Write tool:
`{save_path}`

The report MUST:
- Start with `# Code Review: {repo_name} - Change #{change_number}` as the first line
- Contain a `## Final Verdict` section with **exactly one** of:
  - `✅ **Approve**`
  - `🔄 **Request Changes**`
  - `💬 **Needs Discussion**`
- Contain a `## Backport Recommendation` section (see Step 6)
- End with `END OF REPORT` on its own line

Use the following structure. Replace every `[placeholder]` with real content.
The `{UPPERCASE}` values are already filled in — copy them verbatim.

---
{report_template}
---

A Python audit will check the format after you write the file. Avoid a retry by
getting the structure right the first time.

---

## Step 6: Backport Recommendation

This step is mandatory. Always write a `## Backport Recommendation` section.

### Bug context (pre-fetched by Python)

{bug_context_text}

### Configured backport target branches (pre-fetched by Python)

The following real branch names were resolved from the configured patterns:

{expanded_backport_branches_text}

### Your task

For each branch above, determine whether the bug being fixed is present:
- If a local triage report is listed in the bug context, prefer its **Bug Introduction Analysis**
  "Affected Branches" table.
- If no triage report is available, use:
  ```bash
  git -C {repo_path} merge-base --is-ancestor <introducing_commit> origin/<branch> \
    && echo "AFFECTED" || echo "NOT_AFFECTED"
  ```

{backport_rules_section}

Write one of these in the report:

**Backport recommended:**
```markdown
## Backport Recommendation

**Recommendation:** ✅ Backport recommended

| Branch | Recommended | Reason |
|--------|-------------|--------|
| stable/2024.2 | ✅ Yes | High severity bug present (triage confirmed) |
| stable/2024.1 | ❌ No | Not affected — introducing commit not in this branch |

**Rationale:** <1-2 sentences referencing severity and applicable rules>
```

**No backport needed:**
```markdown
## Backport Recommendation

**Recommendation:** ❌ No backport needed

**Rationale:** <reason — new feature, stable branches unaffected, low severity, etc.>
```

**Insufficient information:**
```markdown
## Backport Recommendation

**Recommendation:** ⚠️ Insufficient information

**Rationale:** <what would be needed to make a recommendation>
```

---

**General instructions:**
- Be thorough and professional
- Provide specific `file:line` references for all issues
- Give actionable suggestions with code examples where helpful
- Balance criticism with recognition of good work
- Follow OpenStack community standards
- DO NOT post to Gerrit — save locally only
- The Python post-flight will restore the original git branch — do NOT run `git checkout` yourself

**Note**: DevStack integration testing is performed asynchronously by a separate agent.
